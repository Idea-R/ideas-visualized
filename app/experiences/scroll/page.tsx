import { ScrollExperience } from "@/components/experiences/ScrollExperience";

export const metadata = { title: "Scroll Experience · Ideas Visualized" };

export default function ScrollExperiencePage() {
  return (
    <main>
      <ScrollExperience />
    </main>
  );
}
